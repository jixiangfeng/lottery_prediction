# 排列五严格逐期前推回测

- 目标期数：`10`
- 最小训练期数：`100`
- 每期候选数：`10`
- 随机基线独立运行：`5` 次
- 嵌套调参：`开启`（内层验证 5 期）
- 对比策略：`current_statistics` 与 `uniform_random`
- 主指标：直选/组选命中、候选联合概率质量、随机基线百分位；位置覆盖仅作诊断。

| 策略 | 直选命中 | 三位彩组选命中 | 候选平均联合分 | 目标平均排名 | 最大连续未中 |
|---|---:|---:|---:|---:|---:|
| current_statistics | 0/10 (0.00%) | - | -8.8176 | 56433.6 | 10 |
| uniform_random | 0.0/10 (0.00%) | - | -12.7584 | - | 10.0 |

## 多随机基线分布

- 直选命中均值 / 5% / 95%：`0.00` / `0.00` / `0.00`
- 直选命中随机百分位：`50.0%`
- 候选联合分随机百分位：`100.0%`

## 嵌套调参证据

- 目标期 `26177`：选择 `marginal_only`，配置训练截止 `26176`。
- 目标期 `26178`：选择 `marginal_only`，配置训练截止 `26177`。
- 目标期 `26179`：选择 `joint_balanced`，配置训练截止 `26178`。
- 目标期 `26180`：选择 `joint_balanced`，配置训练截止 `26179`。
- 目标期 `26181`：选择 `joint_heavy`，配置训练截止 `26180`。
- 目标期 `26182`：选择 `joint_heavy`，配置训练截止 `26181`。
- 目标期 `26183`：选择 `marginal_only`，配置训练截止 `26182`。
- 目标期 `26184`：选择 `marginal_only`，配置训练截止 `26183`。
- 目标期 `26185`：选择 `marginal_only`，配置训练截止 `26184`。
- 目标期 `26186`：选择 `marginal_only`，配置训练截止 `26185`。

## 各位置命中覆盖（诊断项）

- `current_statistics`：万位 6/10 (60.00%)、千位 5/10 (50.00%)、百位 7/10 (70.00%)、十位 6/10 (60.00%)、个位 5/10 (50.00%)
- `uniform_random`：万位 6.4/10 (64.00%)、千位 6.6/10 (66.00%)、百位 5.6/10 (56.00%)、十位 6.4/10 (64.00%)、个位 5.6/10 (56.00%)

## 相对随机基线

- 直选命中率差：`+0.00%`
- 位置覆盖率差：`-3.20%`

> 说明：严格逐期前推避免了目标期之后数据参与训练，但历史结果仍不代表未来；本工具不能保证提高中奖概率。
