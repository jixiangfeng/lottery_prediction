# 福彩3D严格逐期前推回测

- 目标期数：`30`
- 最小训练期数：`100`
- 每期候选数：`10`
- 随机基线独立运行：`5` 次
- 嵌套调参：`开启`（内层验证 5 期）
- 对比策略：`current_statistics` 与 `uniform_random`
- 主指标：直选/组选命中与命中随机基线百分位；位置覆盖和候选分位仅作诊断。

| 策略 | 直选命中 | 三位彩组选命中 | 候选平均复合模型分 | 目标平均排名 | 最大连续未中 |
|---|---:|---:|---:|---:|---:|
| current_statistics | 0/30 (0.00%) | 0/30 (0.00%) | -6.2292 | 444.1 | 30 |
| uniform_random | 0.8/30 (2.67%) | 0.8/30 (2.67%) | -9.4745 | - | 24.2 |

## 多随机基线分布

- 直选命中均值 / 5% / 95%：`0.80` / `0.00` / `1.80`
- 直选命中随机百分位：`20.0%`
- 选择器内部诊断 candidateScorePercentile：`100.0%`
- 诊断解释：使用 mid-rank；当所有随机运行的候选均分打平时，mid-rank 50% 是正常结果，不代表预测优势。
- 组选命中随机百分位：`10.0%`

## 嵌套调参证据

- 目标期 `2026157`：选择 `joint_heavy`，配置训练截止 `2026156`。
- 目标期 `2026158`：选择 `joint_heavy`，配置训练截止 `2026157`。
- 目标期 `2026159`：选择 `joint_balanced`，配置训练截止 `2026158`。
- 目标期 `2026160`：选择 `joint_balanced`，配置训练截止 `2026159`。
- 目标期 `2026161`：选择 `marginal_only`，配置训练截止 `2026160`。
- 目标期 `2026162`：选择 `marginal_only`，配置训练截止 `2026161`。
- 目标期 `2026163`：选择 `marginal_only`，配置训练截止 `2026162`。
- 目标期 `2026164`：选择 `marginal_only`，配置训练截止 `2026163`。
- 目标期 `2026165`：选择 `marginal_only`，配置训练截止 `2026164`。
- 目标期 `2026166`：选择 `marginal_only`，配置训练截止 `2026165`。
- 目标期 `2026167`：选择 `marginal_only`，配置训练截止 `2026166`。
- 目标期 `2026168`：选择 `joint_heavy`，配置训练截止 `2026167`。
- 目标期 `2026169`：选择 `joint_heavy`，配置训练截止 `2026168`。
- 目标期 `2026170`：选择 `joint_heavy`，配置训练截止 `2026169`。
- 目标期 `2026171`：选择 `joint_heavy`，配置训练截止 `2026170`。
- 目标期 `2026172`：选择 `joint_heavy`，配置训练截止 `2026171`。
- 目标期 `2026173`：选择 `marginal_only`，配置训练截止 `2026172`。
- 目标期 `2026174`：选择 `marginal_only`，配置训练截止 `2026173`。
- 目标期 `2026175`：选择 `marginal_only`，配置训练截止 `2026174`。
- 目标期 `2026176`：选择 `marginal_only`，配置训练截止 `2026175`。
- 目标期 `2026177`：选择 `marginal_only`，配置训练截止 `2026176`。
- 目标期 `2026178`：选择 `joint_heavy`，配置训练截止 `2026177`。
- 目标期 `2026179`：选择 `joint_heavy`，配置训练截止 `2026178`。
- 目标期 `2026180`：选择 `joint_heavy`，配置训练截止 `2026179`。
- 目标期 `2026181`：选择 `joint_heavy`，配置训练截止 `2026180`。
- 目标期 `2026182`：选择 `joint_heavy`，配置训练截止 `2026181`。
- 目标期 `2026183`：选择 `joint_heavy`，配置训练截止 `2026182`。
- 目标期 `2026184`：选择 `joint_heavy`，配置训练截止 `2026183`。
- 目标期 `2026185`：选择 `joint_balanced`，配置训练截止 `2026184`。
- 目标期 `2026186`：选择 `joint_heavy`，配置训练截止 `2026185`。

## 各位置命中覆盖（诊断项）

- `current_statistics`：百位 12/30 (40.00%)、十位 14/30 (46.67%)、个位 10/30 (33.33%)
- `uniform_random`：百位 17.8/30 (59.33%)、十位 19.0/30 (63.33%)、个位 17.4/30 (58.00%)

## 相对随机基线

- 直选命中率差：`-2.67%`
- 位置覆盖率差：`-20.22%`

> 说明：严格逐期前推避免了目标期之后数据参与训练，但历史结果仍不代表未来；本工具不能保证提高中奖概率。
