# 排列三严格逐期前推回测

- 目标期数：`30`
- 最小训练期数：`100`
- 每期候选数：`10`
- 随机基线独立运行：`5` 次
- 嵌套调参：`开启`（内层验证 5 期）
- 对比策略：`current_statistics` 与 `uniform_random`
- 主指标：直选/组选命中与命中随机基线百分位；位置覆盖和候选分位仅作诊断。

| 策略 | 直选命中 | 三位彩组选命中 | 候选平均复合模型分 | 目标平均排名 | 最大连续未中 |
|---|---:|---:|---:|---:|---:|
| current_statistics | 0/30 (0.00%) | 1/30 (3.33%) | -5.6800 | 541.8 | 30 |
| uniform_random | 0.0/30 (0.00%) | 2.0/30 (6.67%) | -8.3387 | - | 30.0 |

## 多随机基线分布

- 直选命中均值 / 5% / 95%：`0.00` / `0.00` / `0.00`
- 直选命中随机百分位：`50.0%`
- 选择器内部诊断 candidateScorePercentile：`100.0%`
- 诊断解释：使用 mid-rank；当所有随机运行的候选均分打平时，mid-rank 50% 是正常结果，不代表预测优势。
- 组选命中随机百分位：`20.0%`

## 嵌套调参证据

- 目标期 `26157`：选择 `joint_balanced`，配置训练截止 `26156`。
- 目标期 `26158`：选择 `joint_balanced`，配置训练截止 `26157`。
- 目标期 `26159`：选择 `marginal_only`，配置训练截止 `26158`。
- 目标期 `26160`：选择 `marginal_only`，配置训练截止 `26159`。
- 目标期 `26161`：选择 `marginal_only`，配置训练截止 `26160`。
- 目标期 `26162`：选择 `joint_heavy`，配置训练截止 `26161`。
- 目标期 `26163`：选择 `joint_balanced`，配置训练截止 `26162`。
- 目标期 `26164`：选择 `joint_balanced`，配置训练截止 `26163`。
- 目标期 `26165`：选择 `joint_balanced`，配置训练截止 `26164`。
- 目标期 `26166`：选择 `joint_heavy`，配置训练截止 `26165`。
- 目标期 `26167`：选择 `joint_heavy`，配置训练截止 `26166`。
- 目标期 `26168`：选择 `joint_heavy`，配置训练截止 `26167`。
- 目标期 `26169`：选择 `joint_heavy`，配置训练截止 `26168`。
- 目标期 `26170`：选择 `marginal_only`，配置训练截止 `26169`。
- 目标期 `26171`：选择 `marginal_only`，配置训练截止 `26170`。
- 目标期 `26172`：选择 `marginal_only`，配置训练截止 `26171`。
- 目标期 `26173`：选择 `marginal_only`，配置训练截止 `26172`。
- 目标期 `26174`：选择 `marginal_only`，配置训练截止 `26173`。
- 目标期 `26175`：选择 `joint_heavy`，配置训练截止 `26174`。
- 目标期 `26176`：选择 `joint_heavy`，配置训练截止 `26175`。
- 目标期 `26177`：选择 `joint_heavy`，配置训练截止 `26176`。
- 目标期 `26178`：选择 `joint_heavy`，配置训练截止 `26177`。
- 目标期 `26179`：选择 `joint_heavy`，配置训练截止 `26178`。
- 目标期 `26180`：选择 `joint_heavy`，配置训练截止 `26179`。
- 目标期 `26181`：选择 `joint_balanced`，配置训练截止 `26180`。
- 目标期 `26182`：选择 `marginal_only`，配置训练截止 `26181`。
- 目标期 `26183`：选择 `marginal_only`，配置训练截止 `26182`。
- 目标期 `26184`：选择 `marginal_only`，配置训练截止 `26183`。
- 目标期 `26185`：选择 `marginal_only`，配置训练截止 `26184`。
- 目标期 `26186`：选择 `marginal_only`，配置训练截止 `26185`。

## 各位置命中覆盖（诊断项）

- `current_statistics`：百位 12/30 (40.00%)、十位 11/30 (36.67%)、个位 13/30 (43.33%)
- `uniform_random`：百位 17.8/30 (59.33%)、十位 21.8/30 (72.67%)、个位 19.0/30 (63.33%)

## 相对随机基线

- 直选命中率差：`+0.00%`
- 位置覆盖率差：`-25.11%`

> 说明：严格逐期前推避免了目标期之后数据参与训练，但历史结果仍不代表未来；本工具不能保证提高中奖概率。
