# 设计决策记录（Decision Record）

## 2026-07-16 数字彩模型级证据与联合约束
- 推荐快照保存每个实际启用子模型的 TopK，复盘产出逐模型命中；日报只用已复盘历史做有门槛、有上限的保守调权。
- 遗漏从单一全历史当前值升级为和频率窗口一致的多窗口截断值，并按窗口权重融合。
- 结构约束使用历史平滑概率而非硬编码奇偶模式；默认软惩罚，硬筛选只通过显式配置启用。
- 组选先聚合无序 key 的有序排列证据，再分别在组三/组六空间内重排 16 个模型分位；形态配额仍保持与随机基线一致。
- 蒙特卡洛按位置顺序使用历史位置对构建条件分布，并用形态平滑概率做接受采样；固定种子保证复现。

## 2026-07-16 数字彩多模型排名融合与真实推荐闭环
- 保留旧复合对数分策略作为 `current_statistics`，新增 `ensemble_voting`：14 个统计特征模型加蒙特卡洛、机器学习排序共 16 个投票器独立排序后，以并列中位排名分位做加权融合。
- 日报默认采用集成投票并输出每个候选的 `ensembleScore`、各子模型 `modelRankPercentiles` 与 Top 10% 票数；旧 JSON 字段继续兼容。
- 严格逐期前推同时比较旧复合策略、集成投票和相同过滤/形态预算的多随机基线，不允许用外层目标期开奖结果调集成权重。
- 新增数字彩推荐快照：每次日报先复盘历史快照，再保存当前候选；复盘目标是源期之后实际存在的第一期开奖，并累计输出直选/组选真实命中率。
- 机器学习采用 sklearn 标准化逻辑回归：逐期构建正样本和固定随机负样本，所有特征都在该目标期之前计算；避免把整段历史统计直接回填到早期样本造成未来信息污染。
- 蒙特卡洛以多窗口位置分布为基础，第三轮已加入位置对条件抽样和形态接受率；它和机器学习输出都只作为排序票。
- 日报加入 30/50/100/300/全历史窗口；严格前推可逐窗口独立计算 Top 候选命中和目标排名分位，按稳定分排序。

## 2026-07-15 数字彩严格前推与确定性全空间评分
- 固定候选历史回放保留为覆盖检查，但策略验证改用严格逐期前推：每个目标期只读取此前历史，并与相同过滤口径的 `uniform_random` 比较。
- 福彩3D/排列三枚举 1000 种、排列五枚举 100000 种完整号码空间；静态和值、跨度和形态使用进程内缓存，位置分数按期重新计算。
- 位置频率采用 30/50/100/300 多窗口加权与弱贝叶斯平滑，遗漏分使用 `log1p` 压缩和封顶，避免全历史稀释与极端遗漏支配。
- 高分池使用确定性多样性惩罚，减少同位置数字和相似号码重复；保留原公开 API、`seed` 参数及报告 JSON 核心字段。
- 三位彩默认排豹子、允许跨度 1 组三并限制其前缀/总量；排列五仅给“三一一/三二”少量防守配额。

## 2025-10-13 Copula 采样、图嵌入与互信息惩罚
- 新增 `src/analysis/copula_sampler.py`，在高级策略中默认随 `advanced_mode>=2` 启用；CLI `--copula_*` 参数支持覆盖样本量、收缩强度、候选倍率与随机种子。
- 训练脚本 `scripts/train_graph_embeddings.py` 基于 PyTorch Skip-gram，支持 `--device auto` 自动选择 CPU/GPU/AMD ROCm，结果写入 `analysis.graph_embedding.cache_file`。
- `feature_enhancer` 引入 `graph_embedding_scores` 与缓存清理函数，综合得分增加图嵌入权重，并在测试中覆盖空/有缓存场景。
- `src/analysis/mutual_information.py` 计算 80×80 互信息矩阵，在 `advanced_number_generation` 中作为多样性扣分，避免高度相关号码集中。
- `Makefile` 新增 `make train-graph`，README/运行指南同步补充；`config/config.yaml` 扩充 `analysis.copula`、`analysis.graph_embedding` 配置项。

## 2025-10-13 Dirichlet 平滑与关联规则
- 在 `src/analysis/feature_enhancer.py` 中引入 Dirichlet-Multinomial 后验得分，并通过 `config.analysis.dirichlet` 控制先验与方差惩罚。
- 新增 `src/analysis/rule_miner.py`，基于 FP-Growth 提供 `--rule_filter` 软/硬模式并缓存规则。
- `kl8_analysis*.py` 接入规则筛选并在 README/文档中暴露 CLI 参数与配置示例。
- `tests/test_rule_miner.py` 补充硬/软模式单元测试，验证违规组合与惩罚权重。

## 2025-10-12 特征增强引擎升级
- 新增 `src/analysis/feature_enhancer.py`，实现“近期动量 + 共现谱”的混合评分，支持 `--feature_mode`。
- `kl8_analysis.py` 与 `kl8_analysis_plus.py` 在高级模式下引入特征得分，于候选筛选阶段叠加权重。
- 提供 `FeatureDebugInfo` 以输出细节，便于调试与文档示例。
- 规划单元测试 `tests/test_feature_enhancer.py` 覆盖动量、共现与空数据边界（若缺失将补充）。

---

## 2025-12-19 多线程执行架构优化（Plus 版本）
### 背景与问题
历史实现中使用多进程并行：
- 存在重复数据下载，浪费网络与 IO 资源。
- 进程间共享状态困难，导致全局变量易被误用。
- 进程模型带来更高的内存开销与启动成本。
- 错误传播与统一处理复杂，定位困难。

### 决策
在 I/O 为主的批量任务场景，采用 `concurrent.futures.ThreadPoolExecutor` 替代 `multiprocessing.Process`。

### 选型对比（要点）
- 共享内存：线程天然共享，进程需要序列化与跨进程通信。
- 启动成本：线程低，进程高。
- GIL 影响：CPU 密集型线程受限，但 I/O 密集型收益明显。
- 错误处理：线程池有统一 Future/异常收集；进程需额外管道与协议。

### 架构设计要点
1. 数据下载去重：统一入口 `download_data_if_needed()`，避免并发重复拉取。
2. 线程安全：必要处使用锁保护共享资源，约束副作用边界。
3. 任务编排：主流程负责构建任务，工作线程仅处理纯函数/无副作用逻辑。
4. 错误治理：使用 `as_completed` 聚合结果，失败与重试策略解耦。

### 实现节点
- 线程池配置：`max_workers` 可配置，结合任务与机器核数动态调整。
- 进度与日志：实时输出成功/失败计数与摘要日志。
- 序列化优化：跨线程传递轻量数据结构，避免不必要的大对象复制。

### 效果（定性）
- 明显减少重复下载与请求抖动。
- 内存占用更稳定，整体吞吐提升。
- 故障可观测性与定位效率提升。

---

## 2025-10-11 架构精简与模块重组
- 精简历史脚本与模型代码，保留 `src/analysis/*` 的核心实现，移除未使用与耦合度高的遗留模块。
- 配置与公共方法下沉至统一位置，减少隐式导入与副作用，改进启动与调试体验。
- 数据抓取模块独立，统一格式校验与缓存策略，提升可维护性与可测试性。
- 工具链与测试重建：补充开发依赖与 Pytest 基础用例，使本地 `make ci` 可重复通过。
- 引入示例/样例数据用于本地演示与 CI，但不提交真实数据集。
# 2026-07-16：数字彩第二轮以复合模型排序和外层未见验证为主

- 决策：多窗口位置边际、所有位置对、形态、和值、跨度统一转换为平滑概率并在对数域加权，遗漏只作小权重辅助。
- 决策：候选先按最高复合模型分的 `score_floor` 建立质量池，再在池内做有限多样性；不再以位置覆盖奖励主导排序。该分数由重叠特征加权形成，不是规范联合概率。
- 决策：三位彩直选保留有序号码，组选按无序 key 聚合所有排列的过滤空间归一化模型质量；旧 `candidates` 继续等于直选。
- 决策：排列五前三位直接复用三位前缀评分函数，后两位及五位结构只追加分数，避免两套公式漂移。
- 决策：外层目标期永不参与配置选择；嵌套调参仅比较三组预定义保守配置，并记录每期训练截止证据。
- 权衡：排列五仍需扫描 10 万静态空间，但评分上下文、号码空间和随机抽样均做缓存/轻量化，内层排名不构造全量候选对象。
- 风险：联合历史概率仍可能过拟合或不稳定；即使历史百分位高于随机，也不能承诺未来中奖。
## 2026-07-16 排列五向量化评分池与模型激活证据
- 保留 10 万完整过滤空间，用 NumPy 缓存静态号码/形态/结构特征，并把每期复合分、16 槽位分位、归一化质量保存为紧凑数组；候选对象只在多样性选择、随机抽样或兼容 API 明确需要时物化。
- 同一统计对象、配置和外部分数对象复用同一个有界评分池；缓存键包含对象身份并保存强引用校验，避免不同期或不同 Monte Carlo/ML 分数串用。外部分数构造时复制为只读映射，评分池仅保留最近 2 项，以覆盖同一期生成与 rank 复用并限制长期前推内存。
- 报告新增 active/available 模型名与数量。14 个统计模型始终提供信号；Monte Carlo/ML 只有产出非中性结果并形成模型候选时才标记 active。为保持 16 槽位和既有分值尺度兼容，inactive 外部槽位仍以中性 `0.5` 分位进入固定权重、固定分母公式，只贡献候选间相同的常数，不提供相对排序信号。策略 id `current_statistics`、`ensemble_voting`、`uniform_random` 保持不变。
- 不改变逐期前推边界：每个目标期的高级模型训练、特征和候选仍只读取该目标期之前的数据。`ensembleScore`、分类器分数与 `compositeModelWeight` 均不称为开奖概率。

## 2026-07-16 数字彩日常统计采用“首次全量、之后增量”快照
- 决策：新增独立 `digit_statistics_snapshot.py`，使用版本化 JSON 状态文件保存全历史聚合、基础窗口计数、有界近期队列、遗漏状态、最新开奖与已处理前缀 SHA-256 摘要；不引入数据库。
- 决策：快照命中时只做规范前缀摘要校验与结果重建；纯追加时只处理新增行，通过 +1/-1 更新窗口计数和转移边界。动态全历史窗口从聚合计数生成，不进入近期队列。
- 决策：理论概率与经验概率分层。理论层按规则签名缓存全空间精确枚举；经验层继续复用既有特征定义与平滑域。理论层明确标记为数学基线而非预测。
- 决策：损坏 JSON、版本/规则/窗口/先验不匹配、历史删减、前缀修正或非追加期号一律全量重建并暴露 `rebuildReason`，不静默使用旧状态。保存采用临时文件、`fsync` 与 `os.replace`。
- 决策：日报默认启用增量快照，并默认关闭当前候选的全历史 hindsight 回放；实盘复盘优先使用开奖前 prediction snapshot。旧 `backtest` JSON 结构保留，新增迁移 metadata。
- 防泄漏边界：严格逐期前推继续对每个目标期独立调用历史截止期统计，不导入或读取日报最新快照。
- 权衡：每次仍允许 O(n) 读取并计算轻量前缀摘要，用于发现历史修正；避免的是全量特征统计、位置对与多窗口概率的重复计算。
- 复审修正：窗口兼容签名只包含固定窗口与 `allHistory` sentinel，日报显式传入扩展全历史窗口之前的固定配置，避免总期数碰撞固定窗口时连续重建。
- 复审修正：概率结果直接由 Counter、样本量和离散域套用 Dirichlet 公式，不再物化 N 长样本列表；前缀摘要仍是 O(n) 轻量校验，但不触发全量统计。
- 复审修正：固定锁文件上的 `fcntl.flock` 与进程内 `RLock` 覆盖读取到 `os.replace` 的完整事务；并发等待期间检测到的合法旧前缀只生成调用方结果，不允许回写覆盖较新快照。日报 Markdown/JSON 也改为原子文本替换。
- 最终复审修正：陈旧严格短前缀保护先于任何可能写入的 compatibility/rebuild 分支执行，覆盖窗口、先验、schema/engine 与 `explicit_rebuild`。旧调用按原配置内存全量计算并返回 `stale_view`，以 `requestedRebuildReason` 保留原不兼容原因，以 `persisted=false,snapshotWritten=false` 明确未落盘；非前缀历史修正和删减不进入该模式。
